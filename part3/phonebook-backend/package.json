{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../.. && cd part2/phonebook && npm run build && cd ../.. && cp -r part2/phonebook/dist part3/phonebook-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push origin main"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
