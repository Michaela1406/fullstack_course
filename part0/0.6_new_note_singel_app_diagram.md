sequenceDiagram
    participant browser
    participant server

browser->>server: POST
https://studies.cs.helsinki.fi/exampleapp/new_note_spa
server-->>browser: {content: "kantarelli", date: "2024-09-03T12:20:08.576Z"}
deactivate server

Note right of browser/server: The browser fetches code from the server which in turn fetches a reference to the HTML form element on the page with ID "notes_form" and registers an event_handler to handle the submit event of the form. The event handler prevents the default GET requests, and instead creates a new note, adds it to the notes list and rerenders the notes list on the page and sends the new note to the server 